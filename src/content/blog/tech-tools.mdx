---
title: Tech & Tools
description: Tech & Tools
pubDate: 2023-08-05T10:29:33.000Z
author: Lukas Zangerl
tags:
  - tech-tools
featured: false
draft: false
updatedDate: 2025-09-19T22:16:32.000Z
---
/\* Scoped styles for Image Optimizer \*/ #neurohack-image-optimizer \* { margin: 0; padding: 0; box-sizing: border-box; } #neurohack-image-optimizer { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; line-height: 1.6; color: #333; } .nio-wrapper { background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); padding: 30px 20px; border-radius: 20px; margin: 40px 0; } .nio-container { max-width: 100%; background: white; border-radius: 20px; padding: 30px; box-shadow: 0 20px 60px rgba(0,0,0,0.15); } .nio-header { text-align: center; margin-bottom: 30px; color: #667eea; } .nio-header h2 { font-size: 2.2em; margin-bottom: 10px; color: #667eea; } .nio-subtitle { color: #888; font-size: 1.1em; } /\* Upload Area \*/ .nio-upload-area { border: 3px dashed #667eea; border-radius: 15px; padding: 50px 20px; text-align: center; background: linear-gradient(135deg, rgba(102, 126, 234, 0.05), rgba(118, 75, 162, 0.05)); cursor: pointer; transition: all 0.3s; position: relative; } .nio-upload-area:hover { border-color: #764ba2; background: linear-gradient(135deg, rgba(102, 126, 234, 0.1), rgba(118, 75, 162, 0.1)); } .nio-upload-area.dragging { background: rgba(102, 126, 234, 0.2); border-color: #667eea; transform: scale(1.02); } .nio-upload-icon { font-size: 3em; margin-bottom: 15px; } .nio-upload-text { font-size: 1.2em; color: #667eea; margin-bottom: 8px; font-weight: 600; } .nio-upload-subtext { color: #999; font-size: 0.9em; } #nio-fileInput { display: none; } /\* Controls \*/ .nio-controls { display: none; margin-top: 25px; padding: 20px; background: #f8f9fa; border-radius: 15px; } .nio-controls.active { display: block; animation: nioFadeIn 0.5s; } @keyframes nioFadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } } .nio-control-group { margin-bottom: 20px; } .nio-control-label { display: block; margin-bottom: 10px; font-weight: 600; color: #555; } .nio-quality-slider { display: flex; align-items: center; gap: 15px; } .nio-slider { flex: 1; -webkit-appearance: none; appearance: none; height: 8px; border-radius: 5px; background: linear-gradient(to right, #667eea 0%, #667eea var(--value), #ddd var(--value)); outline: none; } .nio-slider::-webkit-slider-thumb { -webkit-appearance: none; appearance: none; width: 20px; height: 20px; border-radius: 50%; background: #667eea; cursor: pointer; box-shadow: 0 2px 5px rgba(0,0,0,0.2); } .nio-slider::-moz-range-thumb { width: 20px; height: 20px; border-radius: 50%; background: #667eea; cursor: pointer; box-shadow: 0 2px 5px rgba(0,0,0,0.2); } .nio-quality-value { font-weight: bold; color: #667eea; font-size: 1.1em; min-width: 45px; } .nio-format-options { display: flex; gap: 10px; flex-wrap: wrap; } .nio-format-btn { padding: 8px 16px; border: 2px solid #ddd; border-radius: 8px; background: white; cursor: pointer; transition: all 0.3s; font-weight: 500; color: #333; } .nio-format-btn:hover { border-color: #667eea; color: #667eea; } .nio-format-btn.active { background: #667eea; color: white; border-color: #667eea; } .nio-size-options { display: grid; grid-template-columns: repeat(auto-fit, minmax(120px, 1fr)); gap: 10px; } .nio-size-option { padding: 12px; border: 2px solid #ddd; border-radius: 8px; cursor: pointer; transition: all 0.3s; text-align: center; background: white; } .nio-size-option:hover { border-color: #667eea; background: rgba(102, 126, 234, 0.05); } .nio-size-option.active { background: #667eea; color: white; border-color: #667eea; } .nio-size-title { font-weight: bold; margin-bottom: 3px; font-size: 0.95em; } .nio-size-desc { font-size: 0.75em; opacity: 0.8; } /\* Process Button \*/ .nio-process-btn { width: 100%; padding: 15px; background: linear-gradient(135deg, #667eea, #764ba2); color: white; border: none; border-radius: 10px; font-size: 1.05em; font-weight: bold; cursor: pointer; transition: all 0.3s; margin-top: 20px; } .nio-process-btn:hover { transform: translateY(-2px); box-shadow: 0 10px 30px rgba(102, 126, 234, 0.4); } .nio-process-btn:disabled { opacity: 0.6; cursor: not-allowed; transform: none; } /\* Results \*/ .nio-results { display: none; margin-top: 30px; padding: 25px; background: #f0f0f0; border-radius: 15px; } .nio-results.active { display: block; } .nio-comparison { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; margin-bottom: 20px; } @media (max-width: 768px) { .nio-comparison { grid-template-columns: 1fr; } } .nio-image-container { background: white; border-radius: 10px; padding: 15px; box-shadow: 0 5px 15px rgba(0,0,0,0.1); } .nio-image-label { font-weight: bold; margin-bottom: 10px; color: #555; display: flex; justify-content: space-between; align-items: center; } .nio-image-preview { width: 100%; height: auto; border-radius: 8px; display: block; } .nio-stats { margin-top: 10px; padding-top: 10px; border-top: 1px solid #eee; font-size: 0.9em; } .nio-stat-row { display: flex; justify-content: space-between; margin-bottom: 5px; } .nio-savings-badge { display: inline-block; padding: 4px 10px; background: linear-gradient(135deg, #00c851, #00ff88); color: white; border-radius: 15px; font-size: 0.85em; font-weight: bold; } .nio-download-section { background: white; border-radius: 10px; padding: 20px; margin-top: 20px; } .nio-download-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(150px, 1fr)); gap: 10px; margin-top: 15px; } .nio-download-btn { padding: 12px; background: white; border: 2px solid #667eea; border-radius: 8px; color: #667eea; font-weight: bold; cursor: pointer; transition: all 0.3s; text-align: center; } .nio-download-btn:hover { background: #667eea; color: white; } .nio-notification { position: fixed; top: 20px; right: 20px; padding: 15px 25px; background: linear-gradient(135deg, #667eea, #764ba2); color: white; border-radius: 10px; box-shadow: 0 5px 15px rgba(0,0,0,0.2); z-index: 10000; animation: nioSlideIn 0.3s ease; } @keyframes nioSlideIn { from { transform: translateX(100%); } to { transform: translateX(0); } }

## 🖼️ Smart Image Optimizer

Compress images without losing quality

📸

Drop images here or click to browse

Supports JPG, PNG, WebP • Max 50MB

🎯 Quality Level

 85%

📦 Output Format

Original JPEG WebP PNG

📏 Resize Options

Original

Keep size

Large

Max 1920px

Medium

Max 1280px

Small

Max 800px

🚀 Optimize Images

### ✨ Optimization Complete!

Original 0 KB

Dimensions: \-

Optimized \-0%

New Size: \-

Download All Sizes

(function() { 'use strict'; // Scoped Image Optimizer const NIOImageOptimizer = { currentFiles: \[\], processedImages: \[\], init() { this.setupEventListeners(); this.updateQualityDisplay(); }, setupEventListeners() { const uploadArea = document.getElementById('nio-uploadArea'); const fileInput = document.getElementById('nio-fileInput'); const qualitySlider = document.getElementById('nio-qualitySlider'); const processBtn = document.getElementById('nio-processBtn'); uploadArea.addEventListener('click', () => fileInput.click()); fileInput.addEventListener('change', (e) => { this.handleFiles(e.target.files); }); uploadArea.addEventListener('dragover', (e) => { e.preventDefault(); e.stopPropagation(); uploadArea.classList.add('dragging'); }); uploadArea.addEventListener('dragleave', (e) => { e.preventDefault(); e.stopPropagation(); uploadArea.classList.remove('dragging'); }); uploadArea.addEventListener('drop', (e) => { e.preventDefault(); e.stopPropagation(); uploadArea.classList.remove('dragging'); this.handleFiles(e.dataTransfer.files); }); qualitySlider.addEventListener('input', () => { this.updateQualityDisplay(); }); qualitySlider.addEventListener('input', (e) => { const value = (e.target.value - e.target.min) / (e.target.max - e.target.min) \* 100; e.target.style.setProperty('--value', value + '%'); }); qualitySlider.dispatchEvent(new Event('input')); document.querySelectorAll('.nio-format-btn').forEach(btn => { btn.addEventListener('click', (e) => { document.querySelectorAll('.nio-format-btn').forEach(b => b.classList.remove('active')); e.target.classList.add('active'); }); }); document.querySelectorAll('.nio-size-option').forEach(option => { option.addEventListener('click', (e) => { document.querySelectorAll('.nio-size-option').forEach(o => o.classList.remove('active')); e.target.closest('.nio-size-option').classList.add('active'); }); }); processBtn.addEventListener('click', () => { this.processImages(); }); document.getElementById('nio-downloadOptimized').addEventListener('click', () => { this.downloadImage(this.processedImages\[0\]); }); document.getElementById('nio-generateSizes').addEventListener('click', () => { this.generateMultipleSizes(); }); }, updateQualityDisplay() { const value = document.getElementById('nio-qualitySlider').value; document.getElementById('nio-qualityValue').textContent = value + '%'; }, handleFiles(files) { this.currentFiles = Array.from(files); if (this.currentFiles.length === 0) return; document.getElementById('nio-controls').classList.add('active'); this.previewImage(this.currentFiles\[0\]); }, previewImage(file) { const reader = new FileReader(); reader.onload = (e) => { const img = new Image(); img.onload = () => { document.getElementById('nio-originalImage').src = e.target.result; document.getElementById('nio-originalSize').textContent = this.formatFileSize(file.size); document.getElementById('nio-originalDims').textContent = \`${img.width} × ${img.height}\`; }; img.src = e.target.result; }; reader.readAsDataURL(file); }, async processImages() { const processBtn = document.getElementById('nio-processBtn'); const processBtnText = document.getElementById('nio-processBtnText'); processBtn.disabled = true; processBtnText.textContent = '⚙️ Processing...'; this.processedImages = \[\]; for (let i = 0; i < this.currentFiles.length; i++) { const file = this.currentFiles\[i\]; const processed = await this.optimizeImage(file); this.processedImages.push(processed); } this.showResults(); processBtn.disabled = false; processBtnText.textContent = '🚀 Optimize Images'; }, async optimizeImage(file) { return new Promise((resolve) => { const reader = new FileReader(); reader.onload = (e) => { const img = new Image(); img.onload = () => { const canvas = document.createElement('canvas'); const ctx = canvas.getContext('2d'); const { width, height } = this.getResizeDimensions(img); canvas.width = width; canvas.height = height; ctx.imageSmoothingEnabled = true; ctx.imageSmoothingQuality = 'high'; ctx.drawImage(img, 0, 0, width, height); const formatBtn = document.querySelector('.nio-format-btn.active'); let outputFormat = formatBtn.dataset.format; if (outputFormat === 'original') { outputFormat = file.type.split('/')\[1\] || 'jpeg'; } const mimeType = outputFormat === 'jpeg' ? 'image/jpeg' : outputFormat === 'webp' ? 'image/webp' : 'image/png'; const quality = document.getElementById('nio-qualitySlider').value / 100; canvas.toBlob((blob) => { const optimizedFile = new File(\[blob\], file.name.replace(/\\.\[^.\]+$/, \`.${outputFormat}\`), { type: mimeType }); resolve({ original: file, optimized: optimizedFile, dataUrl: canvas.toDataURL(mimeType, quality), width: width, height: height, format: outputFormat, savings: ((file.size - blob.size) / file.size \* 100).toFixed(1) }); }, mimeType, quality); }; img.src = e.target.result; }; reader.readAsDataURL(file); }); }, getResizeDimensions(img) { const sizeOption = document.querySelector('.nio-size-option.active'); const size = sizeOption.dataset.size; if (size === 'original') { return { width: img.width, height: img.height }; } const maxSizes = { large: 1920, medium: 1280, small: 800, thumbnail: 400 }; const maxSize = maxSizes\[size\]; const ratio = Math.min(maxSize / img.width, maxSize / img.height, 1); return { width: Math.round(img.width \* ratio), height: Math.round(img.height \* ratio) }; }, showResults() { const results = document.getElementById('nio-results'); results.classList.add('active'); const first = this.processedImages\[0\]; document.getElementById('nio-optimizedImage').src = first.dataUrl; document.getElementById('nio-optimizedSize').textContent = this.formatFileSize(first.optimized.size); document.getElementById('nio-savingsBadge').textContent = \`-${first.savings}%\`; results.scrollIntoView({ behavior: 'smooth', block: 'nearest' }); }, downloadImage(processedImage) { const link = document.createElement('a'); link.download = processedImage.optimized.name; link.href = processedImage.dataUrl; link.click(); }, async generateMultipleSizes() { const sizes = \['small', 'medium', 'large'\]; const originalFile = this.currentFiles\[0\]; for (const size of sizes) { document.querySelectorAll('.nio-size-option').forEach(o => o.classList.remove('active')); document.querySelector(\`\[data-size="${size}"\]\`).classList.add('active'); const processed = await this.optimizeImage(originalFile); const link = document.createElement('a'); link.download = originalFile.name.replace(/\\.\[^.\]+$/, \`-${size}.${processed.format}\`); link.href = processed.dataUrl; link.click(); await new Promise(resolve => setTimeout(resolve, 200)); } this.showNotification('All sizes generated! 🎉'); }, formatFileSize(bytes) { if (bytes === 0) return '0 Bytes'; const k = 1024; const sizes = \['Bytes', 'KB', 'MB', 'GB'\]; const i = Math.floor(Math.log(bytes) / Math.log(k)); return parseFloat((bytes / Math.pow(k, i)).toFixed(2)) + ' ' + sizes\[i\]; }, showNotification(message) { const notification = document.createElement('div'); notification.className = 'nio-notification'; notification.textContent = message; document.body.appendChild(notification); setTimeout(() => notification.remove(), 3000); } }; // Initialize when DOM is ready if (document.readyState === 'loading') { document.addEventListener('DOMContentLoaded', () => NIOImageOptimizer.init()); } else { NIOImageOptimizer.init(); } })();

### Tech & Tools I use

Speakers: [CREATIVE Pebble V3](https://amzn.to/4fT4iDj)

### Digital Tools

*   [Shortform: My favourite App for Book Summaries](/shortform-my-favorite-app-for-book-summaries/)
*   [My Favourite Tool to make Digital Highlights and store them in One Central Place](/my-favourite-tool-to-make-digital-highlights-and-store-them-in-one-central-place/)

### CMS Ghost

*   [The Best Way To Get Started Learning About How To Build A Ghost Theme](/how-to-build-a-ghost-theme/)
*   [Building a Blog with Ghost CMS (Overview)](/ghost-org-overview/)

### Apps

*   [Top 4 Favourite Apps I Have On My Phone](/favourite-apps-phone/)
*   [My Favourite Apps](/my-favourite-apps-podcast/)
*   [Top Tools, Apps & Books](/my-favourite-apps/)
