---
export { getStaticPaths } from '../../tag/[tag].astro';

const { tag } = Astro.params;
if (!tag) {
  throw new Error('Missing tag parameter');
}

return Astro.redirect(`/tag/${encodeURIComponent(tag)}`);
---
